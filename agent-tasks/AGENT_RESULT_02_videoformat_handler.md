# Результат: обработчик RCVideoFormatSet

## Краткий ответ
Имя ключа параметра — **`Resolution`**.
Калибровка на `RCISOSet` дала ключ `ISO`, метод подтверждён.

## 0. Санити-чек разметки
Адрес `0x006b866c` был проверен: строка по этому адресу — `"RCVideoFormatSet"`.
Таблица по адресу `0x006b86ac` начинается со следующих записей:
- Entry 0: `0x006b8492` -> `0x00154d2c`
- Entry 1: `0x006b849e` -> `0x00154fc8`
- Entry 2: `0x006b842a` -> `0x001550e8`

Санити-чек пройден успешно.

## 1. Обработчик 0x0015641c (полный декомпилированный код + разбор)
Ниже представлен декомпилированный код обработчика `RCVideoFormatSet`:

```c
undefined4 FUN_0015641c(undefined4 param_1,undefined4 param_2,int param_3)

{
  int iVar1;
  undefined4 uVar2;
  int iVar3;
  int unaff_r5;
  char in_NG;
  undefined1 in_ZR;
  char in_OV;
  undefined8 uVar4;
  
  FUN_00384d90();
  iVar3 = 1;
  do {
    FUN_00156aec();
    if (!(bool)in_ZR && in_NG == in_OV) {
      return 0xffffffff;
    }
    iVar1 = FUN_0015699c();
    if ((bool)in_ZR) {
      in_OV = SBORROW4(param_3,10);
      in_NG = param_3 + -10 < 0;
    }
    if ((bool)in_ZR && param_3 == 10) {
      iVar1 = FUN_001900c0(iVar1 + unaff_r5,uRam00156520);
      in_NG = iVar1 < 0;
      in_OV = '\0';
      if (iVar1 == 0) {
        if (iVar3 < 0) {
          return 0xffffffff;
        }
        uVar4 = FUN_00156a68();
        uVar2 = FUN_00154c44((int)uVar4,(int)((ulonglong)uVar4 >> 0x20),0xe);
        FUN_001569c0(uVar2,uRam00156524,0x25);
        return 0xffffffff;
      }
    }
    in_ZR = 0;
    iVar3 = iVar3 + 1;
  } while( true );
}
```

Разбор:
1. **Какие строковые константы он использует?**
Внутри функции есть обращение к указателю `uRam00156520`. Дамп памяти показывает, что там лежит адрес `0x006b8416`.
- По адресу `0x006b8416` находится строка `"Resolution"`. Это и есть ожидаемое имя ключа JSON.
2. **Есть ли сравнение длины строки с константой?**
Да. В коде есть проверка: `if ((bool)in_ZR && param_3 == 10)`. Длина строки `"Resolution"` равна 10. `param_3` содержит длину распарсенного ключа.
3. **Какие значения он принимает?**
В самом обработчике **отсутствуют** строковые константы вроде `4K_30` и `FHD_30`. При совпадении ключа вызывается `FUN_001569c0(..., uRam00156524, 0x25)`. `uRam00156524` содержит указатель на оперативную память (`0xc1514b9c`). `ГИПОТЕЗА:` таблица допустимых значений (строк) инициализируется в RAM при загрузке системы, и проверка значения выполняется внутри `FUN_001569c0`.
4. **Какие ветки ведут к возврату ошибки?**
Если перебираемый ключ не совпал с `"Resolution"`, цикл продолжается. Ошибка (`return 0xffffffff`) возвращается после перебора всех ключей в запросе: `if (!(bool)in_ZR && in_NG == in_OV) { return 0xffffffff; }`. Следовательно, 404 означает отсутствие правильного ключа.

## 2. Калибровка: RCISOSet (0x00155a08) — какой ключ дал мой метод
Декомпилированный код `RCISOSet`:
```c
undefined4 FUN_00155a08(undefined4 param_1,undefined4 param_2,int param_3)

{
  int iVar1;
  undefined4 uVar2;
  int iVar3;
  int unaff_r5;
  char in_NG;
  undefined1 in_ZR;
  char in_OV;
  undefined8 uVar4;
  
  FUN_00384d90();
  iVar3 = 1;
  do {
    FUN_00156aec();
    if (!(bool)in_ZR && in_NG == in_OV) {
      return 0xffffffff;
    }
    iVar1 = FUN_0015699c();
    if ((bool)in_ZR) {
      in_OV = SBORROW4(param_3,3);
      in_NG = param_3 + -3 < 0;
    }
    if ((bool)in_ZR && param_3 == 3) {
      iVar1 = FUN_001900c0(iVar1 + unaff_r5,uRam00155ae8);
      in_NG = iVar1 < 0;
      in_OV = '\0';
      if (iVar1 == 0) {
        if (iVar3 < 0) {
          return 0xffffffff;
        }
        uVar4 = FUN_00156a68();
        uVar2 = FUN_00154c44((int)uVar4,(int)((ulonglong)uVar4 >> 0x20),9);
        FUN_001569c0(uVar2,uRam00155aec,0x1d);
        return 0xffffffff;
      }
    }
    in_ZR = 0;
    iVar3 = iVar3 + 1;
  } while( true );
}
```
**Вывод:**
Функция ищет длину ключа `3` и сверяет строку с указателем `uRam00155ae8`. Дамп показывает, что там лежит адрес `0x006b83c8`. Строка по этому адресу — `"ISO"`.
Калибровка пройдена успешно — метод даёт правильный ключ.

## 3. RCImageAspect (0x00155754)
Декомпилированный код `RCImageAspect`:
```c
undefined4 FUN_00155754(undefined4 param_1,undefined4 param_2,int param_3)

{
  int iVar1;
  undefined4 uVar2;
  int iVar3;
  int unaff_r5;
  char in_NG;
  undefined1 in_ZR;
  char in_OV;
  undefined8 uVar4;
  
  FUN_00384d90();
  iVar3 = 1;
  do {
    FUN_00156aec();
    if (!(bool)in_ZR && in_NG == in_OV) {
      return 0xffffffff;
    }
    iVar1 = FUN_0015699c();
    if ((bool)in_ZR) {
      in_OV = SBORROW4(param_3,0xb);
      in_NG = param_3 + -0xb < 0;
    }
    if ((bool)in_ZR && param_3 == 0xb) {
      iVar1 = FUN_001900c0(iVar1 + unaff_r5,uRam00155ab4);
      in_NG = iVar1 < 0;
      in_OV = '\0';
      if (iVar1 == 0) {
        if (iVar3 < 0) {
          return 0xffffffff;
        }
        uVar4 = FUN_00156a68();
        uVar2 = FUN_00154c44((int)uVar4,(int)((ulonglong)uVar4 >> 0x20),3);
        FUN_001569c0(uVar2,uRam00155ab8,0x17);
        return 0xffffffff;
      }
    }
    in_ZR = 0;
    iVar3 = iVar3 + 1;
  } while( true );
}
```
**Вывод:**
Функция ищет длину `11` (`0xb`). Указатель `uRam00155ab4` ведёт на `0x006b838a`, где лежит строка `"ImageAspect"`.

## 4. Структурные отличия
Структурных отличий между `0x0015641c` (RCVideoFormatSet) и рабочими `0x00155a08` (RCISOSet) / `0x00155754` (RCImageAspect) **нет**.
Во всех трёх случаях:
1. Вызывается `FUN_00384d90()`.
2. В цикле перебираются ключи с проверкой их длины и побайтовым сравнением имени ключа через `FUN_001900c0`.
3. При совпадении вызываются методы `FUN_00156a68()`, `FUN_00154c44` и `FUN_001569c0`.
4. Раннего выхода по проверке состояния (например, идёт ли запись) в самом теле декомпилированных обработчиков нет.
`ГИПОТЕЗА:` отказ с 404 был связан исключительно с неверным ключом.

## 5. Готовые к отправке JSON-запросы
1. `{"command":"RCVideoFormatSet","Resolution":"4K_30"}`
   Обоснование: Самое ожидаемое разрешение, присутствует в вашем списке примеров.
2. `{"command":"RCVideoFormatSet","Resolution":"FHD_30"}`
   Обоснование: Наиболее стандартный режим съемки Full HD, поддерживаемый практически всеми камерами.
3. `{"command":"RCVideoFormatSet","Resolution":"4K_24"}`
   Обоснование: Кинематографичный стандарт 4K, часто используется как альтернатива 30 fps.
4. `{"command":"RCVideoFormatSet","Resolution":"FHD_60"}`
   Обоснование: Стандартный режим для более плавной съемки (60 кадров).

## 6. КРИТИКА СОБСТВЕННОГО ВЫВОДА
Три причины, по которым мой вывод может быть неверным:
1. **Декомпиляция возврата при успехе:** Ghidra декомпилировала выход из ветки совпадения ключа как `return 0xffffffff;` во всех трёх функциях. `ГИПОТЕЗА:` Возможно, декомпилятор ошибся с Control Flow, и там находится не возврат ошибки (`-1`), а вызов функции без возврата (longjmp) или ветвление, которое в реальности отличается для `RCVideoFormatSet`.
2. **Значения хранятся в RAM:** Сами допустимые значения (например, `"4K_30"`) не присутствуют в виде строковых литералов внутри обработчика, а скорее всего валидируются по таблице, на которую указывает RAM-указатель `uRam00156524` (`0xc1514b9c`). `ГИПОТЕЗА:` Если эта таблица не заполняется или пуста (например, из-за специфичного режима работы камеры или нереализованной фичи), команда вернёт ошибку даже с правильным ключом `"Resolution"`.
3. **Логика внутри `FUN_001569c0`:** Обработчик `RCVideoFormatSet` вызывает `FUN_001569c0(..., uRam00156524, 0x25)`, в то время как `RCISOSet` вызывает её с идентификатором `0x1d`. `ГИПОТЕЗА:` Внутри `FUN_001569c0` может быть жёсткий фильтр, который проверяет текущее состояние камеры по переданному идентификатору (`0x25`) и возвращает отказ на уровне ниже, невидимом в текущем дизассемблере.
